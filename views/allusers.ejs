<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Info</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f8f9fa;
        }
        .wrapper {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px; /* space between cards */
        }
        .container {
            background: #ffffff;
            padding: 20px; /* Adjusted padding */
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 400px; /* Increased max width for the cards */
            text-align: center;
            flex: 1 1 400px; /* Adjusted flex-basis to match the max width */
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        h1 {
            font-size: 32px;
            margin-bottom: 20px;
            color: #333333;
            text-align: center;
            width: 100%;
        }
        .info {
            margin: 10px 0; /* Adjusted margin */
            font-size: 18px; /* Font size for info */
            color: #555555;
            word-wrap: break-word; /* Ensures text stays within the container */
        }
        #map {
            width: 100%;
            height: 200px; /* Adjusted height */
            margin-top: 10px; /* Adjusted margin-top */
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border: 4px solid #e9ecef; /* Light gray border */
        }
        iframe {
            width: 100%;
            height: 100%;
            border: 0;
        }
    </style>
</head>
<body>
    <h1>User Information</h1>
    <div class="wrapper">
        <% results.forEach(function(user) { %>
        <div class="container">
            <div class="info"><strong>Name:</strong> <%= user.name %></div>
            <div class="info"><strong>Email:</strong> <%= user.email %></div>
            <div class="info"><strong>Salary:</strong> $<%= user.salary %></div>
            <div id="map">
                <iframe src="https://maps.google.com/maps?q=<%= user.latitude %>,<%= user.longitude %>&z=15&output=embed" frameborder="0" allowfullscreen></iframe>
            </div>
        </div>
        <% }); %>
    </div>
</body>
</html>
